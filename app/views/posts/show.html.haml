- title(@post.title)
- keywords(@post.tags.map(&:name).join(', '))
- description(@post.description)

.wrapper
  = render 'posts/post', :post => @post, :comments => true

  .aside
    = render 'shared/archive'
    = render 'shared/editors'
