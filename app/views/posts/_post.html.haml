%article.post
  %header
    .gravatar
      = link_to image_tag(gravatar_url(post.editor.email)), editor_posts_path(post.editor.id)
    .meta
      %h2= link_to post.title, post
      %div
        %time{:datetime => post.published_at}
          = time_ago_in_words post.published_at
          ago
        %span.tags= raw post.tags.map{|tag| link_to(tag.name, tag_posts_path(tag), :rel => 'tag')}.join(' ')
        %span= link_to pluralize(post.comments_count, 'Comment'), post_path(post) + '#comments'

  %section
    = raw find_and_preserve(post.content)
