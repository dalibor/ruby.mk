- title "Ruby Macedonia"

%section.about
  %p
    Welcome to Ruby Macedonia (a.k.a. MKRUG) – a community for anyone who is interested in the Ruby programming language.
    %br
    We meet up once a month, usually last Thursday in the month with tech presentation and occasional meetups in pub.
    %br
    Join our #{link_to 'mailing list', 'http://groups.google.com/group/mk-ruby-users-group/', :target => '_blank'} for discussions or tweet us #{link_to "@mk_rug", "http://twitter.com/mk_rug", :target => "_blank"} to suggest a presentation and provide feedback.

.section.left
  %section.posts-excerpt
    %header.hidden
      %h2 Recent posts
      = link_to "View all", posts_path

    - @posts.each do |post|
      %article.post
        %header.main-title
          .name= link_to post.editor.name, editor_posts_path(post.editor.id)
          .avatar= link_to image_tag(gravatar_url(post.editor.email, 80)), editor_posts_path(post.editor.id)
          .arrow-right

        %h3.post-title= link_to post.title, post
        %p
          = simple_format truncate html_to_plain_text(post.content), length: 350

        %footer.info
          %time
            = post.published_at.to_s(:long)
          = link_to "read more", post


  = link_to "View all blog posts →", posts_path

.aside.right
  = render 'shared/calendar'
  = render 'shared/archive'

